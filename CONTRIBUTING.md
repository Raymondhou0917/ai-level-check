# 協作指南 Contributing

謝謝你想讓這個專案變得更好。

這個專案要有用，靠的是**各行各業的人把自己領域的判準補進來**。
工程師的 AI 用法跟行銷、設計、客服、會計完全不一樣，
一個人寫不出適用所有職能的判準。

---

還沒做的事集中在 [ROADMAP.md](ROADMAP.md)。先對一下你要補的是不是清單上的項目，避免兩個人做同一件事。

## 提交前先想一件事

> 我要補的，是「新的判準」，還是「現有判準的說法不同」？

例如「有沒有先給完成標準」和「有沒有講清楚驗收條件」是同一件事。
判準要少而準，不追求窮舉。

只有滿足下面至少一條，才建議新增：

- 這個行為在你的領域**會改變判斷結果**，但現有判準抓不到
- 現有判準在你的領域會**誤判**（把正常做法判成弱點）
- 這是一個新來源的 collector

---

## 四種最有價值的貢獻

### 1. 新來源的 collector（最需要）

目前支援 Claude Code、Codex、Antigravity。Cursor、Gemini CLI、
公司內部工具都還沒有。還沒做完的項目見 [ROADMAP.md](ROADMAP.md)。

寫法見 [references/log-sources.md](references/log-sources.md#想加一個新來源)。

**送 PR 時必須附上**：

- 該來源欄位分布的**實測數字**（掃過幾個檔案、各值多少）
- human / automation / subagent 的分流規則，以及為什麼這樣寫
- 一份跑出來的 `metrics.md` 範例（自己的資料，記得脫敏）

沒有實測數字的 collector 不會合併。猜出來的欄位會讓所有下游判斷一起錯。

### 2. 領域判準（第二需要）

你的職能裡，「用得好」長什麼樣？

例如：

- 客服：有沒有先確認客戶情緒與事實，再讓 AI 產草稿
- 設計：有沒有把品牌規範餵進去，還是每次口頭描述
- 會計：有沒有對 AI 算出來的數字做二次核對
- 業務：有沒有把 AI 產的內容當成初稿而不是定稿

開一個 Issue，標題寫 `[判準] <職能>`，內容包含：

- 這個職能的典型 AI 使用場景
- 「做得好」的具體行為，以及**在紀錄裡長什麼樣**（這點最重要）
- 「做不好」的具體行為，以及為什麼那在這個職能會出事
- 一個現有判準會誤判的例子

### 3. 誤判回報

報告把某件事判錯了——說你沒做某件事但你做了，或把正常做法說成弱點。

用 [misjudgement issue 模板](.github/ISSUE_TEMPLATE/misjudgement.md)。
這比 star 有用得多。

### 4. 人物志型錄

16 型的描述目前是初版。如果你覺得某一型的「常見盲點」或「下一步」
不準確、或不適用你的領域，直接提 PR 改。

**一條硬規則**：任何對型錄的修改，都不能讓分型讀起來像分數。
「這型比較弱」「這型需要進步」這類寫法不會合併。

---

## PR 規範

- 一個 PR 做一件事
- 標題用中文，開頭標分類：`[collector]` `[判準]` `[型錄]` `[文件]` `[修正]`
- 改到 `references/` 的判準時，說明「改之前會誤判什麼，改之後不會」
- 改到 `scripts/` 時，附上改動前後跑同一份資料的數字對照

---

## 不接受的貢獻

| 不收 | 為什麼 |
| :-- | :-- |
| 加入分數、百分位、排名功能 | 見 [privacy.md](references/privacy.md#2-不排名不百分位不自動化決定) |
| 讓報告可以被集中掃描或遠端收集 | 那是監控，不是自助檢核 |
| 用提示詞長度、對話輪數、工具數量當能力指標 | 見 [evidence-rules.md](references/evidence-rules.md#公平判斷) 第 2 條 |
| 把分型寫成有高低之分 | 分型是風格 |
| 沒有實測數字的 collector | 猜出來的欄位會讓下游全錯 |
| 加入自動推送、自動上傳 | 會越過「本人看過才推」那一關 |

這幾條不是暫時的取捨，是這個專案存在的前提。
如果你需要的是排名工具，這個專案幫不上忙，那是另一種產品。

---

## 開發環境

沒有依賴，Python 3.9+ 就能跑。

```bash
python3 scripts/collect.py --days 7 --no-content --out /tmp/check
bash -n scripts/publish.sh
```

改完 `collect.py` 之後，至少要用自己的真實紀錄跑一次，
確認「對話分流」那一段的 human 數量合理——這是最容易壞掉的地方。
